const Url={
    baseurl:"http://localhost:3002",   //backend api url
    username:localStorage.getItem("username"), //usrename of logged in user
    id:localStorage.getItem("id"),   //id of logged in user
}
export default Url  